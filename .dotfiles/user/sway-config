# User and machine specific configuration for sway
#
# Use this to setup multiple monitors or custom input settings

# Example commands:
# - scale output: `output some-output scale 1.2`
# - bind output: `workspace $ws1 output some-output`
# - move workspace to output: see below

# Move focused workspace to output
mode 'move-workspace' {
	# Define numbers for outputs. E.g.:
	#bindsym 1 move workspace output <primary-output>; $dreturn
	#bindsym 2 move workspace output <secondary-output>; $return
}
